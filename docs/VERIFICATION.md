# Verification Checklist

## Build
- `npm run build`

## Manual QA
- Header shows a single language switcher
- Logo marquee appears once (duplicate list only for marquee animation)
- Demo CTA renders once per section
- Admin editors load when `/admin` is enabled
- No console errors on landing page
