
  # Clone Sloneek for Echo Pulse

  This is a code bundle for Clone Sloneek for Echo Pulse. The original project is available at https://www.figma.com/design/T1xWh6kUFVAqrMewzfjMny/Clone-Sloneek-for-Echo-Pulse.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  